apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: unbound-combo-blocklist-pvc
  labels:
    project: unbound-combo
spec:
  # https://kubernetes.io/docs/concepts/storage/persistent-volumes/#access-modes-1
  accessModes:
    # - ReadOnlyMany
    # - ReadWriteOnce
    - ReadWriteMany
  resources:
    requests:
      storage: 128Mi
